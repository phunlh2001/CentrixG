/**
 * Single re-export point for the Prisma 7 generated client.
 *
 * The client is generated (by the `prisma-client` generator) into
 * `src/generated/prisma` — outside `node_modules`. Import `PrismaClient`,
 * the `Prisma` namespace, model types (`Product`, `User`, `Token`) and
 * enums (`Role`) from here rather than deep-linking into the generated
 * folder, so the generated path lives in exactly one place.
 */
export * from '../generated/prisma/client';
